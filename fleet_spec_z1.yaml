- name: fleet-spec-z1
  fleetSpec:
    replicas: 1
    template:
      metadata:
        labels:
          foo: bar
      spec:
        ports:
        - name: default
          portPolicy: Dynamic
          containerPort: 7654
        template:
          spec:
            containers:
            - name: game-server-z1
              image: gcr.io/globalgame/game-server-z1:v1

